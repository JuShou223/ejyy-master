# Plan: <feature name>

> Generated from the spec, reviewed by a human, THEN executed. Each step should
> be small enough to finish well under 50% context. Check boxes as you go — this
> is the human-gated task list the delivery loop reads.

## Goal
One sentence. Links: [spec](./SPEC_<feature>.md), issue/PR, design.

## Acceptance criteria (copied from spec — the definition of done)
- [ ]
- [ ]

## Affected / key files
- `path/to/file` — what changes and why

## Risks & unknowns
- Risk → mitigation
- Anything that might need a human decision mid-flight

## Steps
> TDD-friendly: write the failing test first where it makes sense.
- [ ] 1.
- [ ] 2.
- [ ] 3.

## Verification
How we prove it works (these should be runnable, not "looks fine"):
- [ ] `<test command>` is green  (also enforced automatically by the run-checks hook)
- [ ] Manual / browser check: …

## Rollback
How to undo if delivery goes wrong (revert commit, feature flag, etc.).
